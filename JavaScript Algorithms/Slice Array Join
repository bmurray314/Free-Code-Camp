function frankenSplice(arr1, arr2, n) {
  //inserts arr1 into arr2 at index n
  let arrSlice1 = arr2.slice(0,n);
  let arrSlice2 = arr2.slice(n,arr2.length+1);
  let newArr = arrSlice1.concat(arr1,arrSlice2);
  return newArr;
}

frankenSplice([1, 2, 3], [4, 5, 6], 1);
